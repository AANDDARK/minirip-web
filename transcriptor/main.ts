export const Transpile = (source: string | null | unknown) => {
    if (source == null) {
        return 'not have minirip';
    } else {
        return 
    }
}

const lexing = (line: string) => {
    switch (line.charAt(1)){
        case '@':
          
    }
}

const parse = (source: string[]) => {
    
}
const getLines = (source: string): string[] => source.trim().split('\n');